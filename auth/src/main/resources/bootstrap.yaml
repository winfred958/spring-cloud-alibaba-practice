server:
  port: 18081
spring:
  application:
    name: auth-service
  cloud:
    nacos:
      config:
        namespace: DEV
        server-addr: "nacos.server.local.xyz:8484"
        group: base
        file-extension: yaml
      discovery:
        server-addr: "nacos.server.local.xyz:8484"

